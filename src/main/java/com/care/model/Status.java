/*
        Enumeration class for Status. which can take a value of ACTIVE or
        INACTIVE.

        Used with jobs, Applications and Members.
*/

package com.care.model;

public enum Status {
    ACTIVE,
    CLOSED,
    EXPIRED,
    SUSPENDED,
}